[09/06, 6:47 am] Ahanima😉: import java.util.Scanner;

class Person {
    protected String name;
    protected String gender;
    protected String address;
    protected int age;

    public Person(String name, String gender, String address, int age) {
        this.name = name;
        this.gender = gender;
        this.address = address;
        this.age = age;
    }
}

class Employee extends Person {
    protected int empId;
    protected String companyName;
    protected String qualification;
    protected double salary;

    public Employee(String name, String gender, String address, int age, int empId, String companyName, String qualification, double salary) {
        super(name, gender, address, age);
        this.empId = empId;
        this.companyName = companyName;
        this.qualification = qualification;
        this.salary = salary;
    }
}

class Teacher extends Employee {
    private String subject;
    private String department;
    private int teacherId;

    public Teacher(String name, String gender, String address, int age, int empId, String companyName, String qualification, double salary, String subject, String department, int teacherId) {
        super(name, gender, address, age, empId, companyName, qualification, salary);
        this.subject = subject;
        this.department = department;
        this.teacherId = teacherId;
    }

    public void display() {
        System.out.println("Name: " + name);
        System.out.println("Gender: " + gender);
        System.out.println("Address: " + address);
        System.out.println("Age: " + age);
        System.out.println("Employee ID: " + empId);
        System.out.println("Company Name: " + companyName);
        System.out.println("Qualification: " + qualification);
        System.out.println("Salary: " + salary);
        System.out.println("Subject: " + subject);
        System.out.println("Department: " + department);
        System.out.println("Teacher ID: " + teacherId);
        System.out.println("---------------------------");
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter the number of teachers: ");
        int n = scanner.nextInt();
        scanner.nextLine(); // Consume the newline character

        Teacher[] teachers = new Teacher[n];

        for (int i = 0; i < n; i++) {
            System.out.println("\nEnter details for Teacher " + (i + 1) + ":");
            System.out.print("Name: ");
            String name = scanner.nextLine();
            System.out.print("Gender: ");
            String gender = scanner.nextLine();
            System.out.print("Address: ");
            String address = scanner.nextLine();
            System.out.print("Age: ");
            int age = scanner.nextInt();
            scanner.nextLine(); // Consume the newline character
            System.out.print("Employee ID: ");
            int empId = scanner.nextInt();
            scanner.nextLine(); // Consume the newline character
            System.out.print("Company Name: ");
            String companyName = scanner.nextLine();
            System.out.print("Qualification: ");
            String qualification = scanner.nextLine();
            System.out.print("Salary: ");
            double salary = scanner.nextDouble();
            scanner.nextLine(); // Consume the newline character
            System.out.print("Subject: ");
            String subject = scanner.nextLine();
            System.out.print("Department: ");
            String department = scanner.nextLine();
            System.out.print("Teacher ID: ");
            int teacherId = scanner.nextInt();
[09/06, 6:51 am] Ahanima😉: import java.util.Scanner;

class Publisher {
    private String name;

    public Publisher(String name) {
        this.name = name;
    }

    public String getName() {
        return name;
    }
}

class Book {
    protected String title;
    protected Publisher publisher;
    protected int year;

    public Book(String title, Publisher publisher, int year) {
        this.title = title;
        this.publisher = publisher;
        this.year = year;
    }

    public void display() {
        System.out.println("Title: " + title);
        System.out.println("Publisher: " + publisher.getName());
        System.out.println("Year: " + year);
    }
}

class Literature extends Book {
    private String genre;

    public Literature(String title, Publisher publisher, int year, String genre) {
        super(title, publisher, year);
        this.genre = genre;
    }

    public void display() {
        super.display();
        System.out.println("Genre: " + genre);
        System.out.println("---------------------------");
    }
}

class Fiction extends Book {
    private String author;

    public Fiction(String title, Publisher publisher, int year, String author) {
        super(title, publisher, year);
        this.author = author;
    }

    public void display() {
        super.display();
        System.out.println("Author: " + author);
        System.out.println("---------------------------");
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Create a publisher
        System.out.print("Enter publisher name: ");
        String publisherName = scanner.nextLine();
        Publisher publisher = new Publisher(publisherName);

        // Create a literature book
        System.out.print("\nEnter literature book title: ");
        String literatureTitle = scanner.nextLine();
        System.out.print("Enter literature book year: ");
        int literatureYear = scanner.nextInt();
        scanner.nextLine(); // Consume the newline character
        System.out.print("Enter literature book genre: ");
        String literatureGenre = scanner.nextLine();
        Literature literatureBook = new Literature(literatureTitle, publisher, literatureYear, literatureGenre);

        // Create a fiction book
        System.out.print("\nEnter fiction book title: ");
        String fictionTitle = scanner.nextLine();
        System.out.print("Enter fiction book year: ");
        int fictionYear = scanner.nextInt();
        scanner.nextLine(); // Consume the newline character
        System.out.print("Enter fiction book author: ");
        String fictionAuthor = scanner.nextLine();
        Fiction fictionBook = new Fiction(fictionTitle, publisher, fictionYear, fictionAuthor);

        // Display the details of the books
        System.out.println("\nDetails of Literature Book:");
        literatureBook.display();

        System.out.println("Details of Fiction Book:");
        fictionBook.display();

        scanner.close();
    }
}
[09/06, 6:54 am] Ahanima😉: import java.util.Scanner;

class Student {
    protected String name;
    protected int rollNumber;

    public Student(String name, int rollNumber) {
        this.name = name;
        this.rollNumber = rollNumber;
    }

    public void display() {
        System.out.println("Name: " + name);
        System.out.println("Roll Number: " + rollNumber);
    }
}

class Sports {
    protected int sportsScore;

    public Sports(int sportsScore) {
        this.sportsScore = sportsScore;
    }

    public void display() {
        System.out.println("Sports Score: " + sportsScore);
    }
}

class Result extends Student, Sports {
    private int academicScore;

    public Result(String name, int rollNumber, int academicScore, int sportsScore) {
        super(name, rollNumber);
        this.academicScore = academicScore;
        this.sportsScore = sportsScore;
    }

    public void display() {
        super.display();
        System.out.println("Academic Score: " + academicScore);
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter student name: ");
        String name = scanner.nextLine();
        System.out.print("Enter student roll number: ");
        int rollNumber = scanner.nextInt();
        System.out.print("Enter academic score: ");
        int academicScore = scanner.nextInt();
        System.out.print("Enter sports score: ");
        int sportsScore = scanner.nextInt();

        Result result = new Result(name, rollNumber, academicScore, sportsScore);
        System.out.println("\nStudent Result:");
        result.display();

        scanner.close();
    }
}
[09/06, 6:58 am] Ahanima😉: import java.util.Scanner;

// Interface with prototypes of area() and perimeter() methods
interface Shape {
    double area();
    double perimeter();
}

// Circle class implementing the Shape interface
class Circle implements Shape {
    private double radius;

    public Circle(double radius) {
        this.radius = radius;
    }

    @Override
    public double area() {
        return Math.PI * radius * radius;
    }

    @Override
    public double perimeter() {
        return 2 * Math.PI * radius;
    }
}

// Rectangle class implementing the Shape interface
class Rectangle implements Shape {
    private double length;
    private double width;

    public Rectangle(double length, double width) {
        this.length = length;
        this.width = width;
    }

    @Override
    public double area() {
        return length * width;
    }

    @Override
    public double perimeter() {
        return 2 * (length + width);
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int choice;

        do {
            System.out.println("Menu:");
            System.out.println("1. Calculate area and perimeter of Circle");
            System.out.println("2. Calculate area and perimeter of Rectangle");
            System.out.println("3. Exit");
            System.out.print("Enter your choice: ");
            choice = scanner.nextInt();

            switch (choice) {
                case 1:
                    System.out.print("Enter the radius of the Circle: ");
                    double radius = scanner.nextDouble();
                    Circle circle = new Circle(radius);
                    System.out.println("Area: " + circle.area());
                    System.out.println("Perimeter: " + circle.perimeter());
                    break;
                case 2:
                    System.out.print("Enter the length of the Rectangle: ");
                    double length = scanner.nextDouble();
                    System.out.print("Enter the width of the Rectangle: ");
                    double width = scanner.nextDouble();
                    Rectangle rectangle = new Rectangle(length, width);
                    System.out.println("Area: " + rectangle.area());
                    System.out.println("Perimeter: " + rectangle.perimeter());
                    break;
                case 3:
                    System.out.println("Exiting the program...");
                    break;
                default:
                    System.out.println("Invalid choice! Please enter a valid option.");
            }

            System.out.println("---------------------------");
        } while (choice != 3);

        scanner.close();
    }
}
